import { ArrowRight, CalendarDays, ChevronRight, Clock3, Copy, Filter, MoreHorizontal, Plus, Search, Sparkles, Users } from "lucide-react";
import { Link } from "wouter";
import { toast } from "sonner";
import PortalChrome from "@/components/PortalChrome";

const products = [{ id: "jeju-east", title: "제주 동부, 바다를 따라 걷는 1박 2일", status: "초안", area: "제주 동부", updated: "오늘 14:20", score: "86", color: "blue" }, { id: "busan-coast", title: "부산의 밤과 골목, 해안 감성 코스", status: "검토 중", area: "부산", updated: "어제 09:12", score: "78", color: "mint" }, { id: "seoul-food", title: "서울 도심에서 만나는 로컬 미식 루트", status: "완료", area: "서울 종로", updated: "08.24 16:40", score: "74", color: "orange" }];

export default function Products() { return <PortalChrome title="관광상품 기획" eyebrow="PRODUCT LAB"><div className="products-intro"><div><h2>트렌드를 <em>상품으로</em> 연결하세요.</h2><p>실제 이동량이 검증한 루트를 바탕으로 새로운 여행상품 초안을 빠르게 만들 수 있습니다.</p></div><button className="product-create" onClick={() => toast.success("새 상품 초안을 만들기 위한 루트를 선택해주세요.")}><Plus size={16} />새 상품 초안</button></div><div className="product-toolbar"><div className="product-search"><Search size={15} /><input placeholder="상품명, 지역 검색" /></div><button><Filter size={14} />전체 상태</button><button><CalendarDays size={14} />최근 수정순</button></div><div className="product-grid">{products.map((product) => <Link href={`/products/${product.id}`} className="product-card" key={product.id}><div className={`product-cover ${product.color}`}><div className="cover-orbit" /><Sparkles size={19} /><span>{product.area}</span></div><div className="product-card-body"><div className="product-status"><span>{product.status}</span><MoreHorizontal size={16} /></div><h3>{product.title}</h3><div className="product-meta"><span><Clock3 size={13} />{product.updated}</span><span><Users size={13} />루트 점수 {product.score}</span></div><div className="product-card-footer"><b>AI 추천 루트 기반</b><ChevronRight size={16} /></div></div></Link>)}</div><div className="product-tip"><div><Sparkles size={20} /></div><span><b>새로운 루트 기회를 발견했나요?</b><small>트렌드 분석에서 급상승 루트를 선택하면 상품 초안으로 바로 가져올 수 있습니다.</small></span><Link href="/trends">트렌드 분석 보기 <ArrowRight size={15} /></Link></div></PortalChrome>; }
